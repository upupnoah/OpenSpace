
export default function About({ pageProps }: { pageProps: any }) {
  // console.log(pageProps);
  // return <div>{pageProps}</div>;
  return (
    <h1 className="text-2xl font-bold text-center">About Noah!!!</h1>
  )
}
